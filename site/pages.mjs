import {content as c} from './content.mjs';
import {icon,mark,report,reconciliation,diagram,railArt} from './art.mjs';
export const esc=s=>String(s).replace(/[&<>"']/g,x=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[x]));
const arrow=icon('arrow',18);
const link=(url,text,cls='button')=>`<a class="${cls}" href="${url}">${text}${arrow}</a>`;
const eyebrow=t=>`<p class="eyebrow">${t}</p>`;
const sectionHead=(tag,title,body='')=>`<div class="section-heading">${eyebrow(tag)}<h2>${title}</h2>${body?`<p class="lede">${body}</p>`:''}</div>`;
const pageHero=(tag,title,body)=>`<header class="page-hero wrap">${eyebrow(tag)}<h1>${title}</h1><p class="lede">${body}</p></header>`;
const cta=()=>`<section class="closing wrap"><div><p class="eyebrow">A better starting point</p><h2>Bring the question<br>your reports can’t answer.</h2></div><div><p>Your systems. Your way of working.<br>One clearer picture.</p>${link('/contact','Start a conversation','button button-dark')}</div></section>`;
const table=()=>`<div class="comparison-scroll" tabindex="0" role="region" aria-label="Compare reporting approaches; scroll horizontally to see all columns"><table><caption class="sr-only">Reporting approaches compared by how information stays current</caption><thead><tr><th scope="col">What matters</th><th scope="col">Spreadsheet exports</th><th scope="col">A basic sync</th><th scope="col" class="our-column">BundleBoB</th></tr></thead><tbody>${c.comparison.map(row=>`<tr>${row.map((v,i)=>i===0?`<th scope="row">${v}</th>`:`<td${i===3?' class="our-column"':''}>${v}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`;
const industries=(compact=false)=>`<div class="industry-grid">${c.industries.map(x=>`<a class="industry-card" href="/solutions#${x.id}"><span class="industry-icon">${icon(x.icon,30)}</span><h3>${x.name}</h3><p>${compact?x.short:x.text}</p><span class="text-link">Explore the fit ${arrow}</span></a>`).join('')}</div>`;
const ownership=()=>`<div class="ownership-grid"><section class="ownership-card"><span class="ownership-symbol">01 / YOUR BUSINESS INFORMATION</span><h3>Yours, always.</h3><ul class="check-list">${c.ownership.yours.map(t=>`<li>${icon('check',18)}${t}</li>`).join('')}</ul></section><section class="ownership-card ownership-accent"><span class="ownership-symbol">02 / THE TECHNOLOGY BEHIND IT</span><h3>Ours to keep working.</h3><ul class="check-list">${c.ownership.ours.map(t=>`<li>${icon('check',18)}${t}</li>`).join('')}</ul></section></div><p class="ownership-note">${c.ownership.explanation}</p>`;
const video=()=>`<figure class="video-card"><video class="process-video" controls loop playsinline preload="none" poster="/assets/video/reconciliation-poster.png" aria-label="Eight-second illustrative reconciliation walkthrough" data-video><source data-src="/assets/video/reconciliation.webm" type="video/webm"><source data-src="/assets/video/reconciliation.mp4" type="video/mp4"><track kind="captions" src="/assets/video/reconciliation.vtt" srclang="en" label="English" default>Your browser does not support this video. <a href="/assets/video/reconciliation.mp4">Download the MP4</a>.</video><figcaption><span>Two records. One visible difference. <b>Illustrative data.</b></span><span>0:08 · Silent film with captions</span></figcaption><p class="video-transcript">Operations includes an approved addition: $196,450. Accounting includes posted costs only: $184,600. BundleBoB surfaces the $11,850 difference for review.</p></figure>`;
export function home(){return `<header class="home-hero"><canvas class="hero-canvas" aria-hidden="true"></canvas><div class="wrap hero-grid"><div class="hero-copy">${eyebrow(c.home.eyebrow)}<h1>Your work moves fast.<br><span>Your numbers<br> arrive late.</span></h1><p class="lede">${c.home.intro}</p><div class="actions">${link('/how-it-works','See how it works','button button-dark')}${link('/contact','Let’s talk','text-link')}</div><div class="hero-note"><span class="status-dot"></span> Built for businesses that run on jobs.</div></div><div class="hero-art">${report()}<div class="hero-art-caption"><span class="cross">+</span> THE WORK AND THE NUMBERS, IN THE SAME FRAME.</div></div></div><div class="wrap hero-bottom"><span>WORK IN PROGRESS</span><span>JOB PROFITABILITY</span><span>FINANCIAL VISIBILITY</span><span class="hero-bottom-end">A clearer close starts here. ${icon('arrow',16)}</span></div></header>
<section class="wrap section outcomes">${c.home.outcomes.map(([n,h,p])=>`<article><span class="index-number">${n} /</span><h2>${h}</h2><p>${p}</p></article>`).join('')}</section>
<section class="section capability-section"><div class="wrap"><div class="heading-row">${sectionHead('The view you’ve been missing','Less chasing numbers.<br>More knowing the job.')}<div class="rail-controls"><button class="icon-button" data-rail-dir="-1" aria-label="Previous capability">${icon('arrow')}</button><button class="icon-button" data-rail-dir="1" aria-label="Next capability">${icon('arrow')}</button></div></div><div class="capability-rail" tabindex="0" role="region" aria-label="Capabilities; use left and right arrow keys or swipe">${c.home.capabilities.map(([label,title,text,art],i)=>`<article class="capability-card"><div class="card-art">${railArt(art)}</div><div class="card-body"><p class="eyebrow">0${i+1} / ${label}</p><h3>${title}</h3><p>${text}</p>${link('/how-it-works#'+art,'Look closer','text-link')}</div></article>`).join('')}</div></div></section>
<section class="section wrap process-section" data-process><div class="process-intro">${sectionHead('From scattered to shared','Keep your systems.<br>Connect the picture.')}</div><div class="process-grid"><div class="process-visual">${diagram()}<p class="process-caption" data-process-label>03 / A shared, reviewable job picture.</p></div><div class="process-steps">${c.home.steps.map(([a,b,d],i)=>`<article class="process-step" data-step="${i}"><span class="step-number">0${i+1}</span><div><p class="eyebrow">${a}</p><h3>${b}</h3><p>${d}</p></div></article>`).join('')}</div></div></section>
<section class="numbers-section"><div class="wrap"><div class="numbers-heading">${eyebrow('A small example. A meaningful difference.')}<p>Illustrative figures from the sample job above.</p></div><div class="numbers-grid"><div><strong data-count="2">2</strong><span>systems telling the story</span></div><div><strong data-count="11850" data-prefix="$">$11,850</strong><span>in approved costs not yet posted</span></div><div><strong data-count="1">1</strong><span>difference surfaced for review</span></div></div></div></section>
<section class="section wrap">${sectionHead('Built around the work','Different businesses.<br>The same blind spot.')} ${industries(true)}</section>
<section class="section wrap compare-home"><div class="heading-row">${sectionHead('Why BundleBoB','Connecting is the start.<br>Keeping it accurate is the work.')} ${link('/why-bundlebob','Why it works','text-link')}</div>${table()}<p class="table-note">A comparison of approaches. BundleBoB capabilities describe the intended service; we are pre-pilot.</p></section>${cta()}`}
export function how(){return `${pageHero('How it works','From two versions<br>to one clear picture.','BundleBoB connects accounting and operations, reconciles records nightly, and turns differences into a reviewable job picture. Your team keeps the tools it already uses.')}<section class="wrap section section-tight"><div class="intro-split"><div><h2>The disagreement<br>is the useful part.</h2><p class="lede">An approved cost is not always a posted cost. A reliable report makes that distinction visible instead of choosing a number and hiding the other.</p><p>In this synthetic example, an approved addition has reached operations but not accounting. The difference is retained for a person to review.</p></div>${reconciliation()}</div></section>
<section class="section tone-section"><div class="wrap">${sectionHead('The mechanics','A repeatable path to clarity.')}<div class="detail-grid"><article id="margin"><span class="index-number">01 / MAP</span><h3>Agree on what a job means.</h3><p>Document job identifiers, cost categories, reporting periods, and ownership. Map source fields into that shared structure, keeping the original records intact.</p><ul><li>Written mapping your team can review</li><li>Explicit rules for missing or duplicate records</li><li>Read-only access to begin</li></ul></article><article id="reconcile"><span class="index-number">02 / COMPARE</span><h3>Compare like with like.</h3><p>Reconcile corresponding records on a nightly schedule. Distinguish posting delays from missing records and inconsistent mappings.</p><ul><li>Both source values and source identifiers</li><li>The rule that explains each difference</li><li>An assigned review step, not a silent overwrite</li></ul></article><article id="wip"><span class="index-number">03 / UNDERSTAND</span><h3>Make work in progress legible.</h3><p>Bring revenue, costs already incurred, commitments, and remaining work into the same conversation. Show when each source last updated.</p><ul><li>Job profitability with a clear cost basis</li><li>Completed work that is not yet billed</li><li>Freshness visible beside the figures</li></ul></article><article id="report"><span class="index-number">04 / MAINTAIN</span><h3>Keep the picture useful.</h3><p>A dedicated engineer works with your CFO, controller, or finance lead in a standing weekly review. Changes to systems and processes feed back into the mappings.</p><ul><li>Review unresolved differences and owners</li><li>Maintain connections through upgrades</li><li>Adjust reporting as the business changes</li></ul></article></div></div></section>
<section class="section wrap">${sectionHead('Watch the logic','Eight seconds. One explained difference.','An authored, procedural animation using synthetic data. It demonstrates the reconciliation concept, not a live customer environment.')}${video()}</section>
<section class="section wrap" id="integrations">${sectionHead('Integration approach','Your existing tools are the starting point.','We scope connections against the access your systems actually provide. A listed system is a scoping example, not a claim of a production connector.')}<div class="integration-grid"><article><h3>Accounting & ERP</h3><p>QuickBooks, Sage, Viewpoint, and other accounting systems.</p><span class="tag">API or agreed export</span></article><article><h3>Operations & work orders</h3><p>Shop management, field service, project, and production systems.</p><span class="tag">Access reviewed during scoping</span></article><article><h3>The spreadsheets between</h3><p>Job mappings, cost estimates, and the working files that fill the gaps.</p><span class="tag">Documented import rules</span></article></div></section>${cta()}`}
export function solutions(){return `${pageHero('Solutions','If you run jobs,<br>you know the gap.','The work happens in one system. The money lives in another. BundleBoB is designed for businesses that need to bring those views together.')}<nav class="wrap jump-links" aria-label="Industries">${c.industries.map(i=>`<a href="#${i.id}">${i.name}${icon('chevron',14)}</a>`).join('')}</nav>${c.industries.map((i,n)=>`<section class="section wrap solution-section" id="${i.id}"><div class="solution-number">0${n+1}</div><div class="solution-copy">${eyebrow(i.name)}<h2>${i.short}</h2><p class="lede">${i.text}</p><div class="tags">${i.labels.map(t=>`<span class="tag">${t}</span>`).join('')}</div>${link('/contact','Discuss your systems','text-link')}</div><aside class="example-card"><span class="industry-icon">${icon(i.icon,40)}</span><p class="eyebrow">A familiar situation · illustrative</p><p>${i.example}</p><div class="example-rule"><span></span> Keep the source. Explain the difference.</div></aside></section>`).join('')}<section class="section wrap"><div class="fit-callout"><h2>Different industry?<br>Start with the problem.</h2><p>If your work carries costs against a job and your reporting arrives too late to help, the conversation is the same.</p>${link('/contact','Tell us what you run')}</div></section>${cta()}`}
export function why(){return `${pageHero('Why BundleBoB','Accuracy is an<br>ongoing responsibility.','Connecting a pair of systems is a project. Keeping their picture of the business aligned takes maintenance, explicit rules, and a regular conversation.')}<section class="section wrap section-tight">${table()}<p class="table-note">Illustrates differences in approach, not measured performance claims or competitor guarantees.</p></section><section class="section tone-section" id="ownership"><div class="wrap">${sectionHead('Clear ownership','Your information stays yours.<br>We keep the technology working.')} ${ownership()}</div></section><section class="section wrap"><div class="intro-split"><div>${eyebrow('The engagement')}<h2>A dedicated role.<br>A standing review.</h2><p class="lede">A dedicated engineer is accountable to your CFO, controller, or finance lead. One monthly engagement covers the platform and the work of keeping it accurate.</p><p>Scope and pricing are agreed after reviewing your systems, access, and reporting needs.</p></div><ol class="rhythm-list"><li><b>Start with the mapping.</b><p>Agree on the job structure, cost rules, permissions, and the first useful report.</p></li><li><b>Review the differences.</b><p>Work through unresolved records with finance and operations. Assign owners and next steps.</p></li><li><b>Stay aligned.</b><p>Review weekly. Maintain connections, handle upgrades, and update mappings as the work changes.</p></li></ol></div></section><section class="section wrap" id="security">${sectionHead('Security & data handling','Clear responsibilities.<br>Before the first record. ',c.compliance.intro)}<div class="compliance-grid">${c.compliance.items.map(([a,b,d])=>`<article>${icon('shield',28)}<h3>${a}</h3><span class="tag">${b}</span><p>${d}</p></article>`).join('')}</div><div class="principles"><h3>The operating requirements</h3><p>Read-only access at the start. Agreed access permissions. Traceable source records. Documented retention and deletion. Human approval for any write-back. These are delivery requirements to verify before processing client data, not claims of an audited production system.</p></div></section>${cta()}`}
export function resources(){return `${pageHero('Resources','A little clarity,<br>before the next close.','Practical guides to the space between operations and accounting. Written to explain the method, using illustrative examples—not customer results.')}<section class="section wrap section-tight"><div class="resource-toolbar"><label for="resource-search">Find a guide</label><input id="resource-search" type="search" placeholder="Search by topic" aria-controls="resource-list"><span id="resource-count" role="status">3 guides</span></div><div class="resource-list" id="resource-list">${c.resources.map((r,i)=>`<article class="resource-card" data-resource="${esc(r.title+' '+r.description+' '+r.category)}"><a href="/resources/${r.id}"><div class="resource-art art-${i}"><span class="resource-art-label">BUNDLEBOB / FIELD GUIDES</span>${i===0?`<div class="resource-pair"><span>$196,450</span><i>≠</i><span>$184,600</span></div>`:i===1?`<div class="resource-lines"><i></i><i></i><i></i><i></i></div>`:`<div class="resource-map"><span>Job 024</span><b>↔</b><span>J-0024</span></div>`}<span class="resource-art-index">0${i+1}</span></div><div class="resource-card-body"><p class="eyebrow">${r.category} / ${r.time}</p><h2>${r.title}</h2><p>${r.description}</p><span class="text-link">Read the guide ${arrow}</span></div></a></article>`).join('')}</div><p class="empty-state" id="resource-empty" hidden>No guides match that search. Try “job,” “mapping,” or “work in progress.”</p></section>${cta()}`}
const articles={
 'when-systems-disagree':[
 ['Start with the question','A job report says one thing. Accounting says another. Before choosing a winner, ask whether the systems are measuring the same thing at the same point in time. A difference can be useful information. Hiding it can make a report look cleaner while making the decision worse.'],
 ['Separate timing from error','Consider a synthetic job with $184,600 in posted costs. Operations includes an approved addition of $11,850, bringing its total to $196,450. Accounting has not posted that addition yet. Both figures describe something real in the example: one is posted cost, the other is cost including an approved commitment. The $11,850 difference should be explained, not erased.'],
 ['Keep both values and both rules','A useful conflict entry carries the job identifier, source record identifiers, each value, each source’s update time, and the rule used to calculate the amount. “Mismatch” is a warning. “Approved addition A-14 is not yet posted” gives someone a place to start.'],
 ['Give the difference an owner','The next step depends on the cause. An unposted addition belongs with the person who manages posting. An unmatched job identifier belongs in the mapping review. A duplicate import belongs in the ingestion process. Keep the original records; record the resolution separately.'],
 ['Make freshness part of the answer','A nightly process can fail. The report should show the last successful source update and reconciliation, and distinguish a current comparison from one based on stale data. A reliable report admits what it does not yet know.'],
 ['Take this into the next review','Choose one disputed job. Write down both source values, their timestamps, and the question each answers. Identify the owner and next action for the difference. That is a more productive starting point than arguing about which dashboard is correct.'],
 ],
 'wip-review':[
 ['What work is actually complete?','Agree on the operational definition of completion. A work order marked done may still have labor, parts, or subcontracted costs to come. Record those outstanding items beside the job rather than treating completion as a financial close.'],
 ['What has not been billed?','Identify completed work with no corresponding invoice. Ask whether the gap is administrative, contractual, or caused by missing information. The next step should have an owner rather than disappearing into a total.'],
 ['Which costs are still arriving?','Separate posted costs from commitments and estimates of remaining work. Do not add them together blindly: a posted invoice may already reduce an open commitment. Document the rule that prevents counting the same cost twice.'],
 ['Are the periods comparable?','A weekly operations report and a calendar-month accounting report can disagree without either being wrong. Put the period boundaries in writing. Keep service dates, approval dates, and posting dates distinct when they answer different questions.'],
 ['What changed since the last review?','Look for the jobs whose projected result moved, then trace the movement to records. End the review with decisions, owners, and follow-up dates. The aim is a better next action, not a prettier packet.'],
 ],
 'mapping-first':[
 ['A shared label is not a shared definition','Two systems can both have a field called “job” and mean different things. One may track a customer order; another may split that order into service visits or production batches. Start by documenting the relationship.'],
 ['Keep an explicit crosswalk','Record which source identifiers belong to each reporting job. In an illustrative mapping, “Job 024” in operations might correspond to “J-0024” in accounting. Treat that mapping as reviewable business information, not a guess hidden in code.'],
 ['Write down the cost rules','Decide which categories are included, how approved changes are treated, and how currencies and reporting periods line up. Specify how to handle missing values. A blank is not automatically zero.'],
 ['Review exceptions before automating them','An unmatched identifier needs a decision. Propose a mapping for review instead of silently creating a new job or merging records that only look similar. The exception list is part of the output.'],
 ['Maintain the mapping as the business changes','New job types, renamed cost categories, and system upgrades can change the meaning of a report. The customer owns the mapping. BundleBoB maintains its application within the platform as part of the ongoing service.'],
 ]};
export function article(id){const r=c.resources.find(r=>r.id===id);return `<header class="article-hero wrap"><a href="/resources" class="text-link back-link">${arrow} All resources</a>${eyebrow(r.category+' / '+r.time)}<h1>${r.title}</h1><p class="lede">${r.description}</p><p class="article-disclosure">A BundleBoB method guide. Examples are illustrative; this is not a customer case study.</p></header><div class="wrap article-layout"><aside class="article-toc"><p class="eyebrow">In this guide</p><nav aria-label="Article contents">${articles[id].map(([h],i)=>`<a href="#part-${i+1}">${h}</a>`).join('')}</nav></aside><article class="prose">${articles[id].map(([h,p],i)=>`<section id="part-${i+1}"><h2>${h}</h2><p>${p}</p>${id==='when-systems-disagree'&&i===1?reconciliation('article-recon'):''}</section>`).join('')}<div class="article-next"><h2>Where do your numbers disagree?</h2><p>Bring the question to a conversation about your systems.</p>${link('/contact','Start with your situation')}</div></article></div>${cta()}`}
export function about(){return `${pageHero('About BundleBoB','The work should be<br>easier to understand.','Businesses have plenty of systems. What they often lack is a current, shared explanation of how the work is affecting the money. That is the gap BundleBoB is built to close.')}<section class="section wrap section-tight"><div class="about-statement"><span class="large-mark">${mark}</span><div><h2>Clarity is a product.<br>Keeping it current is a service.</h2><p class="lede">BundleBoB brings job profitability, work in progress, and operational reporting together across the tools a business already uses.</p><p>The platform does the connecting and comparing. A dedicated engineer helps your finance and operations teams agree on the rules and keeps the picture aligned as the business changes.</p></div></div></section><section class="section tone-section"><div class="wrap">${sectionHead('What guides the product','A few things we won’t make complicated.')}<div class="principle-grid"><article><span>01</span><h3>Show the source.</h3><p>A figure should lead back to the records and rules that explain it.</p></article><article><span>02</span><h3>Keep the difference.</h3><p>When systems disagree, preserve the evidence and make the next step clear.</p></article><article><span>03</span><h3>Respect the work.</h3><p>Start with how the business operates. Keep the tools its people already know.</p></article></div></div></section><section class="section wrap"><div class="status-panel"><span class="tag">Pre-pilot</span><h2>Built around a method.<br>Measured through real work.</h2><p>BundleBoB is preparing for its first client pilots. The illustrations on this site use synthetic data. The guides explain the approach; they do not represent customer outcomes.</p>${link('/how-it-works','Explore the method','text-link')}</div></section>${cta()}`}
export function contact(config){return `${pageHero('Contact',c.contact.title,c.contact.intro)}<section class="section wrap section-tight contact-layout"><div><div class="contact-info">${eyebrow('A useful first conversation')}<h2>No need to arrive<br>with a perfect brief.</h2><p>Which systems do you use? Where does the reporting fall behind? What would you like to understand about a job before it finishes?</p><ul class="check-list"><li>${icon('check',18)} A conversation about the fit</li><li>${icon('check',18)} A review of the systems involved</li><li>${icon('check',18)} A clear next step</li></ul>${config.email?`<p class="contact-direct">Email us at<br><a class="text-link" href="mailto:${esc(config.email)}">${esc(config.email)}</a></p>`:`<div class="contact-route"><b>Prepare an inquiry to share.</b><p>Download your inquiry and share it through your existing introduction. No information is sent from this page.</p></div>`}${config.booking?link(config.booking,'Book a conversation'):''}</div></div><form id="inquiry-form" novalidate data-email="${esc(config.email||'')}"><div class="form-heading"><h2>Your starting point</h2><p>Fields marked * are required.</p></div><div class="form-row"><div class="field"><label for="name">Your name *</label><input id="name" name="name" autocomplete="name" required maxlength="100" aria-describedby="name-error"><span id="name-error" class="field-error"></span></div><div class="field"><label for="company">Business name *</label><input id="company" name="company" autocomplete="organization" required maxlength="160" aria-describedby="company-error"><span id="company-error" class="field-error"></span></div></div><div class="field"><label for="email">Work email *</label><input id="email" name="email" type="email" autocomplete="email" required maxlength="254" aria-describedby="email-error"><span id="email-error" class="field-error"></span></div><div class="field"><label for="systems">The systems you use</label><input id="systems" name="systems" placeholder="Accounting, work orders, spreadsheets…" maxlength="240"></div><div class="field"><label for="message">Where does the picture break down? *</label><textarea id="message" name="message" required minlength="10" maxlength="1000" rows="4" aria-describedby="message-error inquiry-privacy"></textarea><span id="message-error" class="field-error"></span></div><p class="form-privacy" id="inquiry-privacy">${c.contact.privacy}</p><button class="button button-dark submit-button" type="submit"><span>Prepare my inquiry</span>${arrow}</button><p class="form-status" role="status" aria-live="polite"></p><div id="inquiry-result" hidden><h3>Your inquiry is ready.</h3><p>${config.email?'Open the prepared email in your mail app, review it, and send it.':'Download it and share it through your existing introduction.'} Nothing has been sent automatically.</p><div class="actions"><a class="button" id="inquiry-download" download="bundlebob-inquiry.txt">${icon('download',18)} Download inquiry</a>${config.email?`<a class="text-link" id="inquiry-email">Open email ${arrow}</a>`:''}</div></div><noscript><p class="no-script-note">The inquiry builder needs JavaScript. ${config.email?`Email ${esc(config.email)} with your business name, systems, and question.`:'Share those details directly with the person who introduced you to BundleBoB.'}</p></noscript></form></section>`}
export function system(){return `${pageHero('Design system / v1.0','Clarity, by design.','Warm surfaces, deliberate contrast, and motion that helps explain the work. The same rules apply from a small label to a full job view.')}<section class="section wrap section-tight"><div class="system-notice">Use the theme control in the navigation to inspect both designed palettes.</div>${sectionHead('01 / Palette','Roles before colors.','Eight paired roles. Contrast values below are computed from the active theme in the browser.')}<div class="swatches">${['paper','surface','ink','muted','accent','accent-ink','positive','line'].map(x=>`<div class="swatch"><i style="background:var(--${x})"></i><b>${x}</b><code data-color="${x}"></code></div>`).join('')}</div><div class="contrast-results" aria-live="polite"></div></section><section class="section wrap">${sectionHead('02 / Typography','Human to read. Precise to scan.')}<div class="type-spec"><div><h3>Native sans serif</h3><p>Avenir Next → Segoe UI → system sans. A generous, familiar voice for business reading. Uses installed fonts; no font assets or remote font requests.</p><h3>Native monospace</h3><p>SFMono-Regular → Consolas → monospace. Reserved for record labels, identifiers, and small wayfinding details.</p></div><div class="type-samples"><p class="type-display">Current by design.</p><p class="type-heading">Know the job.</p><p class="type-body">A useful number has a source.</p><p class="eyebrow">JOB 024 / NIGHTLY REVIEW</p><code>Display 44–80 · H2 32–52 · H3 22–28 · Body 16–18 · Label 11–12 px</code></div></div></section><section class="section wrap">${sectionHead('03 / Space','Room for the decision.')}<div class="space-scale">${[4,8,12,16,24,32,48,64,96].map(n=>`<div><i style="width:${n}px"></i><code>${n}px</code></div>`).join('')}</div><p class="lede">A 1,240px content width, 32px desktop gutters, and 20px mobile gutters. Two-column stories collapse at 900px; cards retain their reading width in the rail. Sections use 96px desktop and 64px mobile spacing.</p></section><section class="section wrap">${sectionHead('04 / Components','A consistent response.')}<div class="component-demo"><div><h3>Actions</h3><div class="actions"><button class="button button-dark" type="button" data-demo="success">Primary action ${arrow}</button><button class="button" type="button" data-demo="loading">Show loading</button><button class="button" disabled>Unavailable</button></div><p class="demo-status" role="status"></p></div><div><h3>Interaction states</h3><div class="state-grid"><span class="button state-hover">Hover</span><span class="button state-focus">Focus</span><span class="button state-press">Pressed</span><span class="button state-loading" aria-label="Loading example">Preparing…</span><span class="state-message success">${icon('check',16)} Ready</span><span class="state-message error">Check this field</span></div></div><div><h3>Fields</h3><div class="form-row"><div class="field"><label for="system-input">Default</label><input id="system-input" placeholder="Business name"></div><div class="field"><label for="system-error">Error example</label><input id="system-error" aria-invalid="true" aria-describedby="system-error-text" value=""><span class="field-error" id="system-error-text">Enter your business name.</span></div></div></div><div><h3>Status and disclosure</h3><div class="tags"><span class="tag">Pre-pilot</span><span class="tag tag-positive">Ready for review</span><span class="tag">Illustrative data</span></div><details><summary>What does illustrative mean?</summary><p>The values explain the method. They do not represent a customer result.</p></details></div></div></section><section class="section wrap">${sectionHead('05 / Motion','Movement with a reason.')}<div class="motion-spec"><article><h3>Ambient field</h3><p>A canvas field reacts to pointer movement. It stops drawing when settled or offscreen. Reduced motion uses the static CSS background.</p><code>Settle: 700ms · cubic ease-out</code></article><article><h3>Reconciliation</h3><p>Inline SVG and CSS reveal two values and the conflict between them, once. Reduced motion shows the complete result.</p><code>Rows: 650ms · conflict: 450ms</code></article><article><h3>Scroll sequence</h3><p>A shared diagram progresses from source records to a reviewable job picture. Reduced motion shows the final diagram and all explanatory steps.</p><code>Damping: 0.12 per reference frame</code></article><article><h3>Feedback</h3><p>Controls share hover, focus, press, busy, success, and error rules. Nothing important is conveyed through movement alone.</p><code>Feedback: 160ms · ease-out</code></article></div>${reconciliation('system-recon')}</section><section class="section wrap">${sectionHead('06 / Patterns','The parts in context.')}<p class="lede">The global navigation and footer demonstrate both themes. The following patterns use the same component rules as the product pages.</p><div class="system-patterns">${report()}${diagram()}</div><div class="capability-rail" tabindex="0" role="region" aria-label="Example capability cards">${c.home.capabilities.map(([label,title,text,art])=>`<article class="capability-card"><div class="card-art">${railArt(art)}</div><div class="card-body"><p class="eyebrow">${label}</p><h3>${title}</h3><p>${text}</p></div></article>`).join('')}</div>${table()}${industries(true)}${video()}</section>`}
export function layout({title,description=c.description,path='/',body,config={},noindex=false}){const nav=[['/how-it-works','How it works'],['/solutions','Solutions'],['/why-bundlebob','Why BundleBoB'],['/resources','Resources']];return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="color-scheme" content="light dark"><title>${esc(title)} | BundleBoB</title><meta name="description" content="${esc(description)}"><link rel="canonical" href="https://bundlebob.com${path}">${noindex?'<meta name="robots" content="noindex,follow">':''}<meta property="og:type" content="website"><meta property="og:site_name" content="BundleBoB"><meta property="og:title" content="${esc(title)}"><meta property="og:description" content="${esc(description)}"><meta property="og:url" content="https://bundlebob.com${path}"><meta property="og:image" content="https://bundlebob.com/assets/og-image.png"><meta property="og:image:width" content="1200"><meta property="og:image:height" content="630"><meta property="og:image:alt" content="BundleBoB. Your work moves fast. Your numbers arrive late."><meta name="twitter:card" content="summary_large_image"><meta name="twitter:image" content="https://bundlebob.com/assets/og-image.png"><link rel="icon" href="/assets/favicon.svg" type="image/svg+xml"><script src="/assets/theme.js"></script><link rel="stylesheet" href="/assets/site.css"><script src="/assets/site.js" defer></script></head><body><a class="skip-link" href="#main">Skip to content</a><header class="site-header"><div class="nav-wrap"><a class="brand" href="/" aria-label="BundleBoB home">${mark}<span>BundleBoB<span class="brand-period">.</span></span></a><nav class="desktop-nav" aria-label="Main navigation">${nav.map(([url,t])=>`<a href="${url}"${path===url?' aria-current="page"':''}>${t}</a>`).join('')}</nav><div class="nav-actions"><button class="theme-toggle icon-button" type="button" aria-label="Switch color theme" title="Switch color theme">${icon('sun',19)}${icon('moon',19)}</button>${link('/contact','Let’s talk','button nav-cta')}<button class="menu-toggle icon-button" aria-controls="mobile-menu" aria-expanded="false" aria-label="Open navigation"><span></span><span></span></button></div></div><nav id="mobile-menu" class="mobile-menu" aria-label="Mobile navigation" hidden>${nav.concat([['/about','About'],['/contact','Let’s talk']]).map(([url,t])=>`<a href="${url}"${path===url?' aria-current="page"':''}>${t}${arrow}</a>`).join('')}</nav></header><main id="main">${body}</main><footer class="site-footer"><div class="wrap footer-grid"><div class="footer-brand"><a class="brand" href="/">${mark}<span>BundleBoB.</span></a><p>The work and the numbers,<br>in the same frame.</p><span class="tag">Pre-pilot · illustrative examples</span></div><div><p class="footer-heading">Explore</p><a href="/how-it-works">How it works</a><a href="/solutions">Solutions</a><a href="/why-bundlebob">Why BundleBoB</a></div><div><p class="footer-heading">Understand</p><a href="/resources">Resources</a><a href="/why-bundlebob#security">Security & data</a><a href="/why-bundlebob#ownership">Ownership & engagement</a></div><div><p class="footer-heading">Company</p><a href="/about">About</a><a href="/contact">Contact</a><a href="/system">Design system</a></div></div><div class="wrap footer-bottom"><span>© ${new Date().getFullYear()} BundleBoB</span><span>Built for the way work actually happens.</span><a href="#main">Back to top ↑</a></div></footer></body></html>`}
