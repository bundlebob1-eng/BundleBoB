import {content as c} from './content.mjs';
import {icon,mark,diagram,railArt} from './art.mjs';

const arrow=icon('arrow',18);
const a=(href,text,cls='button button-dark')=>`<a href="${href}" class="${cls}">${text}${arrow}</a>`;
const eyebrow=text=>`<p class="eyebrow">${text}</p>`;
const photos=['workshop','fabrication','field-service','fabrication'];
const alts=['A shop owner and technician reviewing work together','A fabrication team reviewing a production job','Field service technicians reviewing their work orders','A team discussing a custom fabrication job'];
const photo=(name,alt,cls='',eager=false)=>`<img class="${cls}" src="/assets/images/${name}.webp" srcset="/assets/images/${name}-768.webp 768w, /assets/images/${name}.webp ${name==='workshop'?1672:1448}w" sizes="${eager?'(max-width: 640px) 1672px, 100vw':'(max-width: 640px) 100vw, 60vw'}" alt="${alt}" width="${name==='workshop'?1672:1448}" height="${name==='workshop'?941:1086}" ${eager?'fetchpriority="high"':'loading="lazy"'} decoding="async">`;

export function closing(){return `<section class="editorial-closing"><div class="wrap closing-grid"><div>${eyebrow('Make the next decision a clearer one')}<h2>Ready to see the<br>bigger picture?</h2>${a('/contact','Let’s talk about your business','button button-white')}</div><div class="closing-image">${photo('field-service',alts[2])}</div></div></section>`;}

export function homepage({video,table}){return `
<header class="home-hero editorial-hero">
  <picture class="hero-media"><source media="(max-width: 640px)" srcset="/assets/images/workshop-mobile.webp">${photo('workshop','A shop owner and technician reviewing a tablet in a workshop','hero-photograph',true)}</picture>
  <div class="hero-shade"></div><canvas class="hero-canvas" aria-hidden="true"></canvas>
  <div class="wrap editorial-hero-content">${eyebrow('Job visibility that keeps your business moving')}
    <h1>Because every job<br> should add up.</h1>
    <div class="actions">${a('/how-it-works','Explore the platform','button button-white')}${a('/contact','Let’s talk','button button-glass')}</div>
  </div><a class="hero-scroll" href="#platform" aria-label="Explore the BundleBoB platform">${icon('chevron',22)}</a>
</header>
<div class="spectrum-rule" aria-hidden="true"></div>
<section class="sector-strip wrap" aria-label="Built for businesses that run on jobs">${c.industries.map(i=>`<a href="/solutions#${i.id}">${icon(i.icon,28)}<span>${i.name}</span></a>`).join('')}</section>
<section class="section capability-section editorial-capabilities" id="platform"><div class="wrap">
  <div class="heading-row"><div class="section-heading">${eyebrow('The BundleBoB platform')}<h2>See more of what<br class="mobile-only"> makes a job work.</h2><p class="lede">Your systems connected. Your next move clearer.</p></div>
  <div class="rail-controls"><button class="icon-button" data-rail-dir="-1" aria-label="Previous capability">${arrow}</button><button class="icon-button" data-rail-dir="1" aria-label="Next capability">${arrow}</button></div></div>
  <div class="capability-rail photo-rail" tabindex="0" role="region" aria-label="Capabilities; use left and right arrow keys or swipe">
  ${c.home.capabilities.map(([label,title,text,art],i)=>`<article class="capability-card photo-capability">${photo(photos[i],alts[i])}<div class="photo-card-shade"></div><div class="photo-card-copy"><h3>${label}</h3><p>${text}</p>${a('/how-it-works#'+art,'Learn more<span class="sr-only"> about '+label.toLowerCase()+'</span>','button button-glass')}</div><div class="photo-card-inset"><span>${title}</span><div class="mini-art">${railArt(art)}</div><small>Illustrative job view</small></div></article>`).join('')}</div>
</div></section>
<section class="goals-section"><div class="wrap goals-grid"><div>${eyebrow('Start with what matters')}<h2>What would you<br>like to see clearly?</h2><p>Your business has its own moving parts. Start with the question that brings them together.</p></div><div class="goal-links">${[
  ['margin','Help me understand which jobs are profitable'],['wip','Help me see my work in progress'],['reconcile','Help me resolve differences between systems'],['report','Help me get a clearer operational report']
].map(([id,t])=>a('/how-it-works#'+id,t,'goal-link')).join('')}</div></div></section>
<section class="section insights-section"><div class="wrap insights-grid"><div>${eyebrow('The difference is in the detail')}<h2>Better visibility.<br>Better conversations.</h2><p class="lede">A useful report explains the numbers. It shows what each system knows, what is missing, and what needs a decision.</p>${a('/resources/when-systems-disagree','Explore the example')}<p class="example-disclosure">Illustrative figures, not customer results.</p></div>
<div class="insight-cards"><article><h3>Connected sources</h3><strong data-count="2">2</strong><p>systems contributing to one job picture</p></article><article><h3>A visible difference</h3><strong data-count="11850" data-prefix="$">$11,850</strong><p>in approved costs not yet posted</p></article><article><h3>A clear next step</h3><strong data-count="1">1</strong><p>difference surfaced for your team to review</p></article><article><h3>A regular rhythm</h3><strong>Nightly</strong><p>reconciliation designed to keep the picture current</p></article></div></div></section>
<section class="industry-stories section"><div class="wrap"><div class="section-heading centered">${eyebrow('Built around your business')}<h2>Different work.<br>The same need for clarity.</h2>${a('/solutions','Explore the industries','button button-white')}</div>
<div class="industry-tabs" aria-label="Choose an industry">${c.industries.map((i,n)=>`<button type="button" data-industry="${n}" aria-controls="industry-story-${n}">${i.name}</button>`).join('')}</div>
<div class="industry-panels">${c.industries.map((i,n)=>`<div class="industry-story" id="industry-story-${n}" data-industry-panel="${n}"><div class="story-copy">${eyebrow('A familiar situation · illustrative')}<h3>${i.short}</h3><p>${i.example}</p><span>${i.name}</span></div><div class="story-photo">${photo(photos[n],alts[n])}<div class="story-photo-caption"><p>${i.text}</p>${a('/solutions#'+i.id,'Explore the fit','button button-glass')}</div></div></div>`).join('')}</div>
</div></section>
<section class="section wrap process-section editorial-process" data-process><div class="process-intro">${eyebrow('Keep your systems. Connect the picture.')}<h2>Clarity is a process.<br>We keep it moving.</h2></div><div class="process-grid"><div class="process-visual">${diagram()}<p class="process-caption" data-process-label>03 / A shared, reviewable job picture.</p></div><div class="process-steps">${c.home.steps.map(([a,b,d],i)=>`<article class="process-step" data-step="${i}"><span class="step-number">0${i+1}</span><div>${eyebrow(a)}<h3>${b}</h3><p>${d}</p></div></article>`).join('')}</div></div></section>
<section class="film-section section"><div class="wrap film-grid"><div>${eyebrow('See the method')}<h2>Two records.<br>One explained<br>difference.</h2><p>The work moves before the books catch up. See how a difference becomes something your team can review.</p>${a('/how-it-works','See how it works')}</div>${video()}</div></section>
<section class="section wrap editorial-comparison"><div class="heading-row"><div class="section-heading">${eyebrow('Why BundleBoB')}<h2>How does your<br>reporting compare?</h2></div>${a('/why-bundlebob','Explore the approach')}</div>${table()}<p class="table-note">A comparison of approaches. BundleBoB is pre-pilot; capabilities describe the intended service.</p></section>
<section class="section wrap editorial-resources"><div class="heading-row"><div class="section-heading">${eyebrow('Resources')}<h2>A fresh perspective<br>on the work.</h2></div>${a('/resources','Explore all resources')}</div><div class="editorial-resource-grid">${c.resources.map((r,i)=>`<article><a href="/resources/${r.id}" class="resource-picture">${i===0?`<div class="editorial-resource-art"><span>WHEN SYSTEMS DISAGREE</span><div><b>$196,450</b><em>≠</em><b>$184,600</b></div><small>Keep the source. Explain the difference.</small></div>`:photo(photos[i],alts[i])}</a><div class="editorial-resource-copy">${eyebrow(r.category)}<h3><a href="/resources/${r.id}">${r.title}</a></h3><p>${r.description}</p>${a('/resources/'+r.id,'Read the guide')}</div></article>`).join('')}</div></section>
${closing()}`;}

const menuGroups=[
  {label:'Product',url:'/how-it-works',title:'See the whole job.',text:'Connect your existing systems and understand the work between them.',links:c.home.capabilities.map(([label,,text,id])=>['/how-it-works#'+id,label,text])},
  {label:'Solutions',url:'/solutions',title:'Built around your work.',text:'One shared picture, whatever your business makes, repairs, installs, or builds.',links:c.industries.map(i=>['/solutions#'+i.id,i.name,i.short])},
  {label:'Resources',url:'/resources',title:'Make room for clarity.',text:'Practical guides for your next operations and finance conversation.',links:c.resources.map(r=>['/resources/'+r.id,r.title,r.category+' · '+r.time])},
  {label:'Company',url:'/about',title:'The work behind the view.',text:'Learn about the purpose, principles, and responsibilities behind BundleBoB.',links:[['/about','About BundleBoB','Why we are building a clearer job picture.'],['/why-bundlebob#ownership','Ownership & engagement','Your information and our ongoing responsibility.'],['/why-bundlebob#security','Security & data','Our preparation and delivery requirements.'],['/contact','Contact','Start with the question your reports cannot answer.']]}
];
export function navigation(path){const menu=g=>`<details class="nav-item"><summary${path===g.url?' class="nav-current"':''}>${g.label}${icon('chevron',12)}</summary><div class="mega-panel"><div class="mega-intro">${eyebrow(g.label)}<h2>${g.title}</h2><p>${g.text}</p>${a(g.url,'Explore '+g.label.toLowerCase(),'text-link')}</div><div class="mega-links">${g.links.map(([url,t,d])=>`<a href="${url}"><span>${t}${arrow}</span><small>${d}</small></a>`).join('')}</div></div></details>`;
return `<header class="site-header"><div class="nav-wrap"><a class="brand" href="/" aria-label="BundleBoB home">${mark}<span>BundleBoB</span></a><nav class="desktop-nav" aria-label="Main navigation">${menu(menuGroups[0])}${menu(menuGroups[1])}<a class="nav-direct" href="/why-bundlebob"${path==='/why-bundlebob'?' aria-current="page"':''}>Why BundleBoB</a>${menu(menuGroups[2])}${menu(menuGroups[3])}</nav><div class="nav-actions"><button class="theme-toggle icon-button" type="button" aria-label="Switch color theme" title="Switch color theme">${icon('sun',19)}${icon('moon',19)}</button>${a('/contact','Let’s talk','button button-white nav-cta')}<button class="menu-toggle icon-button" aria-controls="mobile-menu" aria-expanded="false" aria-label="Open navigation"><span></span><span></span></button></div></div><nav id="mobile-menu" class="mobile-menu" aria-label="Mobile navigation" hidden>${[['/how-it-works','Product'],['/solutions','Solutions'],['/why-bundlebob','Why BundleBoB'],['/resources','Resources'],['/about','Company'],['/contact','Let’s talk']].map(([url,t])=>`<a href="${url}"${path===url?' aria-current="page"':''}>${t}${arrow}</a>`).join('')}</nav></header>`;}

export function footer(){const column=(title,links)=>`<div><p class="footer-heading">${title}</p>${links.map(([href,t])=>`<a href="${href}">${t}</a>`).join('')}</div>`;return `<footer class="site-footer"><div class="wrap footer-grid"><div class="footer-brand"><a class="brand" href="/">${mark}<span>BundleBoB</span></a><p>See the work.<br>Understand the numbers.</p><p class="footer-stage">Pre-pilot · Illustrative examples</p></div>${column('Platform',[['/how-it-works','Overview'],...c.home.capabilities.map(([label,,,id])=>['/how-it-works#'+id,label])])}${column('Industries',c.industries.map(i=>['/solutions#'+i.id,i.name]))}${column('Resources',[['/resources','All guides'],['/resources/wip-review','WIP review checklist'],['/why-bundlebob','Compare approaches']])}${column('Company',[['/about','About'],['/contact','Contact'],['/why-bundlebob#ownership','Ownership'],['/why-bundlebob#security','Security & data']])}</div><div class="wrap footer-bottom"><span>© ${new Date().getFullYear()} BundleBoB</span><span>Made for businesses that run on jobs.</span><a href="/system">Design system</a><a href="#main">Back to top ↑</a></div><div class="wrap imagery-note">Business scenes are AI-generated illustrations, not customer photographs.</div></footer>`;}
