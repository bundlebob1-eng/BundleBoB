# BundleBoB studio direction

September 2026 revision: present BundleBoB as a technology service partner for practical AI, custom software, and integrations. Construction is a specialty, supported by the existing reconciliation demonstration. No customer names, shipped-project claims, testimonials, or results are invented.

The homepage moves through services, recognizable pain points, a construction demonstration, the engagement process, working principles, and contact. Services and approach have dedicated routes. Existing reporting pages explain the construction use case.

Warm ivory, charcoal green, muted sage, and burnt orange replace the photographic gradient direction. Montserrat carries the interface; native Georgia italic adds selective emphasis. The hero uses CSS perspective and layered geometry, with pointer-driven tilt and a reduced-motion fallback. Native disclosures remain keyboard-operable without scripts.

Before launch, supply verified construction case studies and a monitored contact destination. The current form prepares a downloadable inquiry locally; it does not send leads. Publishing and DNS are not changed by this revision.

## Review

Run `npm start` and visit `http://127.0.0.1:8080`. `npm run verify` covers all 13 routes in both themes and two viewport sizes. `node scripts/editorial-check.mjs` additionally covers seven widths, keyboard navigation, CSS model interaction, reduced motion, and native disclosures with JavaScript disabled. `node scripts/accessibility.mjs` runs the automated accessibility audit. Browser scripts support the installed Chrome channel as documented in the README.
