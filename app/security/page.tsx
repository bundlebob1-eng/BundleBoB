import type { Metadata } from "next";
import Link from "next/link";

export const metadata: Metadata = {
  title: "Security & data",
  description:
    "How a client firm's accounting, job-cost, and subcontractor data is isolated, stored, accessed, and handled — stated plainly, including what is not in place yet.",
};

const SECTIONS = [
  {
    h: "Tenancy and isolation",
    intro:
      "Each client firm's data is kept separate from every other client's — in storage, in processing, and in anything an AI model sees. Nothing built or learned for one firm is reused in another firm's context, prompts, or reports.",
    detail:
      "In practice: each firm's data is stored and queried under its own credentials, and every read, report and model prompt is scoped to a single firm. There is no shared context that could carry one client's numbers into another's. The full architecture is available for review under NDA.",
  },
  {
    h: "Encryption",
    intro: "Client data is encrypted in transit and at rest.",
    detail:
      "Transport uses current industry-standard TLS; data at rest is encrypted with industry-standard ciphers. The specific protocol versions, key management, and hosting provider and region are set out in our security documentation and shared on request.",
  },
  {
    h: "Access — who can see what",
    intro:
      "Access is limited to the Forward Deployed Engineer assigned to your firm and named individuals who support them. Access is least-privilege and logged.",
    detail:
      "Only the FDE on your engagement, plus named colleagues who directly support it, can reach your data. We do not use your data for benchmarking, model training, marketing, or any purpose other than your engagement. Access is granted and revoked on your instruction, and the access log is available to you on request.",
  },
  {
    h: "AI and your data",
    intro:
      "Figures shown to you are computed from your source records, not generated by a model. Where an LLM is used to draft answers or summaries, it runs against your isolated data only, and its output is reviewed before anyone acts on it.",
    detail:
      "A model never originates a number — displayed figures are computed directly from your records, with the underlying line items attached. Where an LLM drafts a summary or answer it runs only against your scoped data, and a person reviews the output before any action is taken. Which providers are used, and how prompt and output retention is handled, is documented and shared on request; we will agree in writing any data category that must never be sent to a model.",
  },
  {
    h: "What is not in place yet",
    intro: "This buyer reads silence as evasion, so we state the gaps directly.",
    detail:
      "As of now there is no SOC 2 report, no third-party penetration test, and no signed BAA. What is in place instead: written data-handling and access policies, the isolation and review model described above, named accountability for every engagement, and a standing offer to complete your security questionnaire before you sign anything. This section is updated as that changes.",
  },
  {
    h: "If an engagement ends",
    intro: "You keep what was built. Your data is returned or destroyed on your instruction.",
    detail:
      "On your instruction, your data is exported back to you in a portable format (CSV or database dump) and then deleted from our systems, with written confirmation. The connections, data models, documentation and code built during the engagement are yours to keep. Backup retention and deletion timelines are set out in the engagement agreement.",
  },
];

export default function Security() {
  return (
    <>
      <section className="border-b border-white/[0.06]">
        <div className="container mx-auto px-5 pb-14 pt-36 md:pb-20 md:pt-44">
          <p className="label">Security &amp; data</p>
          <h1
            className="mt-5 max-w-[26ch] font-syne font-semibold tracking-[-0.03em] text-white"
            style={{ fontSize: "clamp(26px,4.4vw,46px)" }}
          >
            We will be inside your accounting and subcontractor data. Here is how that is handled.
          </h1>
          <p className="mt-5 max-w-[60ch] text-[15px] leading-[1.7] text-white/50">
            This page is written to be checkable. Where a control is real, it is stated plainly. Where it is not
            in place yet, that is stated too, next to what is true instead.
          </p>
        </div>
      </section>

      {SECTIONS.map((s) => (
        <section key={s.h} className="border-b border-white/[0.06]">
          <div className="container mx-auto grid grid-cols-1 gap-8 px-5 py-12 md:grid-cols-[280px_1fr] md:py-16">
            <h2 className="font-syne text-[19px] font-semibold text-white">{s.h}</h2>
            <div className="max-w-[64ch] space-y-4">
              <p className="text-[14px] leading-[1.75] text-white/50">{s.intro}</p>
              <p className="text-[14px] leading-[1.75] text-white/80">{s.detail}</p>
            </div>
          </div>
        </section>
      ))}

      <section>
        <div className="container mx-auto px-5 py-20 md:py-28">
          <h2
            className="max-w-[28ch] font-syne font-semibold tracking-[-0.03em] text-white"
            style={{ fontSize: "clamp(20px,3vw,30px)" }}
          >
            Send us your security questionnaire. We would rather answer it before you ask than after.
          </h2>
          <Link href="/contact" className="btn btn-shine mt-8">How to reach us</Link>
        </div>
      </section>
    </>
  );
}
